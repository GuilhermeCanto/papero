# Public demo deployment mode.
# No database or auth secrets are required.
# Demo finance data is stored only in each visitor's browser localStorage.
NEXT_PUBLIC_PAPERO_DATA_MODE="demo"
PAPERO_DATA_MODE="demo"
