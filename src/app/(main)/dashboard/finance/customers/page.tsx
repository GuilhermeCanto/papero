import { ContactDirectoryPage } from "../_components/contact-directory-page";

export default function CustomersPage() {
  return <ContactDirectoryPage contactType="customer" copyKey="customers" />;
}
